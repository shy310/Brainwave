# VITE_API_URL is intentionally blank — frontend and backend are served from the same origin.
# The frontend calls /api/claude as a relative URL which the Express server handles.
VITE_API_URL=

# Set GROQ_API_KEY in Railway dashboard as a runtime environment variable — never commit it.
